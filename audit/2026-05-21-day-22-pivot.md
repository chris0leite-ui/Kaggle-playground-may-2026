# Day-22 (2026-05-21 PM) — Plateau pivot + pseudo-label + FT-Transformer push

Comp-day 21 of 31 → calling this "Day 22" in friction conventions, ISO date
2026-05-21. Session arc: R31 3-way submit (TIE confirmed), per-cohort isotonic
NULL, R31 (Driver,Y,R) standalone NULL, R32 (Y,R,Stint) GPU group-size limit,
R33 (Y,R) deferred (concurrent limit), pseudo-label R34 + FT-Transformer R35
pushed.

## Today's submission

| Round | CSV | OOF | LB | Δ vs R25 | Verdict |
|---|---|---|---|---|---|
| R31 | K18+R30+R72 0.50/0.15/0.35 rank_mean | 0.954529 | 0.95402 | +0.021 / 0 bp | LB-TIE confirmed |

Submission count: 53 of 270 total. 3 used 2026-05-21 (R25, R27, R31). 7 daily
slots remaining at writing.

## Strategy-critic Section 5 ran (plateau + 65% checkpoint triggers)

Re-ran headroom math with full-LB context (downloaded Kaggle public LB CSV):

| Boundary | Score | Gap vs R25 |
|---|---|---|
| Top-1% | 0.95453 | +5.1 bp |
| Top-5% | 0.95449 | +4.7 bp |
| Top-10% | 0.95420 | +1.8 bp |
| Our rank | 297 / 2063 | top-14.4% |

HANDOVER's "top-5% gap -0.7 bp" was stale (computed early when LB was less
mature). The public-kernel cluster at 0.95453 (77 teams) is the real
benchmark, driven by `flexonafft/f1-submission-blender-0-9545` (75 votes)
which ensembles strong public kernels (`safar1/lb-score-0-95449` etc.).

**Own-lever discounted EV** (per Strategy-critic Section 5):
- Pseudo-labels: 0.30 × 0.3 = 0.09 bp
- FT-Transformer: 0.20 × 1.0 = 0.20 bp
- More R21 hyperparam variants: 0.50 × 0.05 = 0.025 bp
- Bag more seeds on R30: 0.80 × 0.05 = 0.04 bp
- K=27 widepool refit R21-arch: 0.30 × 0.10 = 0.03 bp
- Public-notebook IDEA scan + reimplement: 0.15 × 1.5 = 0.225 bp

Sum: 0.61 bp; 50% additivity discount: **0.30 bp**.

**Verdict**: top-5% mathematically unreachable on own-lever set alone
(need +4.7 bp vs realistic discounted lift ~0.30 bp). Top-10% (+1.8 bp)
requires high-success outcomes on top-2 levers.

PI directive: **pseudo-labels + FT-Transformer in parallel**. Pushed:
- `kernels/r34-pseudolabel-yetirank-gpu` — K=18 threshold-based pseudo-label
  (>=0.85 → 1; ≤0.02 → 0; ~80k pseudo rows) → retrain CB YetiRank on K=17
  with (Y,R,L) cohort.
- `kernels/r35-ft-transformer-gpu` — feature-tokenizer transformer over 9
  num + 4 cat (raw F1) + 17 K17 logit tokens; 3 layers × 96 dim × 8 heads;
  structurally orthogonal to r5/r6 sequence-transformers (lap-axis) since
  R35 does feature-axis self-attention.

## Earlier today (R30, R31 inventor, R32 fail, isotonic NULL)

### R30 — 5-seed bag of R21
- OOF AUC 0.954027 (R21 standalone was 0.953956; +0.07 bp from variance reduction)
- ρ vs R21 = 0.99879; ρ vs K18 = 0.97951
- Nested-CV K18+R30 2-way ~= R25 (no LB lift expected)
- Nested-CV K18+R30+R72 3-way: +0.021 bp OOF vs R25; weights stable (0.50/0.15/0.35)
- Submitted as R31; LB tied (confirmed TIE_ZONE-extended pattern).

### R31 — CB YetiRank on (Driver, Year, Race) cohort
- Cohort: 40,869 groups, median 11 laps/group
- OOF AUC 0.951779 — 22 bp below K18. **Too weak for blend ingredient.**
- ρ vs K18 = 0.9704 (most orthogonal cohort variant) but standalone gap kills lift.
- Mechanism: (Driver, Y, R) ranks "when within driver's race did they pit" — sequence-time
  ordering, not row-AUC-aligned. R21's (Y,R,L) is uniquely metric-aligned.

### R32 — CB YetiRank on (Y, R, Stint) cohort: ERROR
- Group max size 5,621 > CB GPU YetiRank's 1,023 limit. NEW FRICTION:
  `cb-gpu-yetirank-max-query-1023` (supersedes 2026-05-21 friction
  `cb-gpu-query-cross-entropy-size-limit` which claimed YetiRank had no limit).
- R33 (Y,R) cohort would have same issue (max 7,908). Deferred indefinitely.

### Per-cohort isotonic calibration of R25 (R33 inner-CV) — NULL
| Cohort | OOF Δ vs R25 |
|---|---|
| (Year, Race) | **-13.5 bp** |
| (Year, Race, Stint) | -43.9 bp |
| (Year, Race, Compound) | -32.1 bp |

CLOSED — per-cohort isotonic catastrophically regresses. Confirms 2026-05-08
friction `isotonic-overfits-when-base-calibrated`. Promotion candidate:
add this to a "permanently-closed mechanisms" list.

### Day-16 GRU revisited as blend ingredient
- OOF 0.93066, ρ vs K18 = 0.9000 (most orthogonal we have)
- K18+GRU best blend: AUC 0.954503 at w=0.99 (i.e., basically K18 alone)
- Closed: gap to GBDT-class (24 bp) exceeds blending utility.

## Frictions surfaced today

- **cb-gpu-yetirank-max-query-1023**: R21's (Y,R,L) cohort max=373 worked; R32's
  (Y,R,Stint) max=5621 errored with `max query size supported on GPU is 1023`.
  Past friction (`cb-gpu-query-cross-entropy-size-limit`) said YetiRank had no
  limit; reality is 1023. Cap cohort definitions accordingly.
- **public-LB-gap-stale-in-handover**: HANDOVER said -0.7 bp from top-5%; actual
  is -4.7 bp. Stale because computed at earlier LB stage. Promotion candidate:
  refresh LB-percentile gap at every session start via downloading full
  public LB CSV.
- **per-cohort-isotonic-needs-explicit-block**: 30 min spent on per-cohort isotonic
  prototype despite 2026-05-08 friction `isotonic-overfits-when-base-calibrated`
  flagging this class. The friction wasn't checked first because cohort wasn't
  Compound or per-gap — but mechanism class is the same. Add to mechanism-ledger
  permanent-closed list with cohort-agnostic note.
- **monitor-until-loop-empty-status-false-positive**: until-loop condition
  `[[ "$(kaggle kernels status ...)" != *RUNNING* ]]` becomes TRUE when kaggle
  CLI returns empty/error string (e.g., transient API failure). Filter with
  grep -oE 'KernelWorkerStatus\.[A-Z_]+' to require a real status string
  before exiting the loop.

## Pending at session end

- R34 (pseudo-label CB YetiRank): RUNNING on Kaggle GPU.
- R35 (FT-Transformer): RUNNING on Kaggle GPU.
- Both should land within next 30-60 min.

## Critical files

- `submissions/submission_R31_K18_R30_R72_rankmean_50_15_35.csv` — LB 0.95402 (TIE)
- `kernels/r34-pseudolabel-yetirank-gpu/` — pending
- `kernels/r35-ft-transformer-gpu/` — pending
- `kernels/r30-r21-bag-cohort-gpu/output/oof_R30_r21_bag_yrl_strat.npy` — copied to scripts/artifacts
- `kernels/r31-cb-yetirank-dyr-cohort-gpu/output/oof_R31_yetirank_dyr_strat.npy` — copied to scripts/artifacts (NULL)
