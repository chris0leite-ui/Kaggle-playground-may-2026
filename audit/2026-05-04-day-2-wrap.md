# Day-2 wrap (2026-05-04)

PI declared EOD with 2/5 slots used today (per new day-end rule:
slot-exhaustion OR PI override). Three slots forfeited to Kaggle UTC
quota — won't recover. Day 3 starts at next UTC midnight with 5 fresh.

## PI summary (3 bullets)

- **M5 LR meta-stacker landed LB 0.94693, +58.0bp over Day-1 baseline
  0.94113.** Headroom to top-5% (0.95345) cut from 123bp → 65bp. Stack
  built from 5-base pool {baseline, d2a_te, m2_xgb, m3_catboost,
  m4_relstate}; recipe = LR on `[raw, rank, logit]` per
  analyticaobscura's notebook (cross-comp Source 1 #2).
- **OOF→LB gap reverses sign on stack:** baseline single-model gap
  +3.8bp (LB > OOF); M5 stack gap −4.4bp (LB < OOF). ~8bp shift toward
  overshoot. Unknown whether this is stack-method-specific (M5 LR) or
  pool-overfit (any blend of these 5 bases). Day-3 S2 (M6 Dirichlet
  submit) will distinguish.
- **Race-overfit is mechanism-dependent:** XGB/CatBoost overfit Race
  hard (GroupKF −97 / −41 bp); relative-state FE doesn't (GroupKF
  +13.6bp). M4 is the *only* Day-2 candidate lifting both anchors.
  Confirms pilkwang/PM-02 caveat: physics-faithful FE on this DGP
  regresses; relative-state generalizes.

## Mechanism families covered today (7 total)

1. baseline_lgbm_raw_features (Day 1)
2. oof_target_encoding (Day 2 D2-A; null standalone, +2-4bp in blend)
3. xgb_native_categorical
4. catboost_native_categorical
5. relative_state_fe
6. lr_meta_stacker_3view
7. dirichlet_random_search (computed, not submitted)

## Day-3 hypothesis board (re-ranked by expected learning per slot)

### Compute-only (no slot, run before UTC midnight)

- **E1 row-subsample CatBoost** (80% bagging, depth=6, iter=800)
  — bound Race-overfit on strongest single. ~15 min.
- **E2 L1-regularized M5** — diagnose stack overshoot. <1 min.
- **E3 HGBC sklearn base** (4th GBDT family) — diversity. ~5 min.
- **E4 RealMLP single-fold CPU probe** (NEW, per relaxed 1-fold-1h
  cap) — research-cited +30-50bp lever; if first fold under 1h and
  AUC promising, queue for D3+ pursuit. ~50 min.
- E5 Refit M5 with expanded pool (post E1/E3/E4) — Day-3 PRIMARY
  candidate.

### Slot-spending submits (5 fresh at UTC midnight, each PI-confirmed)

- **S1 M3 CatBoost standalone** — single-model-other-than-baseline
  gap calibration. Highest learning per slot.
- **S2 M6 Dirichlet** — distinguishes stack-method overfit from
  pool-overfit. M6 weights are sparse (M3=65%) vs M5's distributed.
- **S3 Refit M5 (post-E1/E3/E4)** — Day-3 PRIMARY, expected ≥0.947.
- **S4 M4 RelState standalone** — R2 hedge data point.
- S5 reserve — late-day learning surprise.

## Friction this session

See `audit/friction.md` (5 entries appended).

## Skill amendments to commit

- `loops.md` Day-loop "Stop" → make slot-exhaustion-or-PI-override
  explicit and binary.
- `loops.md` Pick-experiment → re-rank by learning-per-slot at every
  replan, not by speculative lift.
- `loops.md` / `guardrails.md` 1h cap → single-fold (not 5-fold
  projection) is the gate; if first fold fits, see results then decide.
- `do-and-dont.md` → DON'T write end-of-day audit until 5/5 used or
  PI declares EOD.
