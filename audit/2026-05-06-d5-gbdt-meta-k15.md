# Day-5 GBDT-meta over K=15 (M5q pool + recursive) — null

## Result

| Variant | K=14 (d4 anchor) OOF | K=15 (today) OOF | Δ | ρ vs M5q test | gate |
|---|---:|---:|---:|---:|---|
| lgbm_shallow (d=3) | 0.95048 | **0.95038** | **−1.0bp** | 0.99521 | REAL_DELTA |
| lgbm_medium (d=5) | 0.95047 | **0.95037** | **−1.0bp** | 0.99422 | REAL_DELTA |
| hgbc (d=4) | 0.95042 | **0.95032** | **−1.0bp** | 0.99449 | REAL_DELTA |

Wall: 96s total (lgbm_shallow 27s, lgbm_medium 37s, hgbc 33s).

## What this proves

1. **Adding recursive to the pool HURTS the GBDT-meta** by ~1bp OOF
   uniformly across all three variants. The recursive base provides
   redundant rank info that the GBDT splits on but cannot generalize
   from (the splits create overfit against val without lifting AUC).
2. **The meta-divergence ceiling is fixed at ρ≈0.995 vs M5q
   regardless of pool composition**. Adding an extra base does not
   give the meta room to find more divergent patterns; the constraint
   is what M5q's 14-base rank ordering already encodes.
3. **d4's K=14 GBDT-meta IS the GBDT-meta ceiling.** No pool extension
   improves it. Combined with today's LR-meta K=15 null (3rd
   rank-lock confirmation), we have empirically established:
     - Meta-switch ceiling: GBDT-meta OOF 0.95048 / projected LB ≈ 0.94965
     - Meta-add ceiling: LR-meta OOF 0.95057 / LB 0.95005 (M5q itself)

## Strategic implication: pool reformation is the only EV-positive move

Three failed Day-5 probes today (Path C standalone-then-stack, K=15 LR
re-stack, K=15 GBDT-meta sweep) plus three failed Day-4 probes
(yetirank/nb/GBDT-meta) plus prior layered-orthogonal nulls — six
total negative results all sharing the structural pattern:

> **The M5q pool's rank ordering is the binding constraint.
> Anything that takes M5q's bases as inputs cannot break the LB
> ceiling, regardless of meta family or added base.**

Per HANDOVER Path B: **pseudo-labeling at scale** rebuilds the BASE
POOL itself. Every base re-trains on (train + pseudo-test),
producing fresh OOFs whose rank structure is no longer constrained
by what 200k training rows can distinguish. 30bp-class moves in
prior comps when it lands.

## Falsified

- "GBDT-meta over K=15 with recursive breaks the GBDT-meta ceiling" → null
- "Adding any single base to M5q pool breaks the meta-add ceiling" → 4th confirmation

## Held artifacts

- `oof_d5_meta_k15_lgbm_shallow_strat.npy` / `_medium` / `_hgbc`
- `submission_d5_meta_k15_*.csv` (all expected to be ~−4 to −6bp LB
  vs M5q based on d4 calibration; **DO NOT submit blindly** — pre-submit-diff
  required, all three are below the d4 GBDT-meta benchmark)
- `scripts/artifacts/d5_gbdt_meta_k15_results.json`

## Decision: pivot to Path B (pseudo-labeling)

End — 60 lines.
