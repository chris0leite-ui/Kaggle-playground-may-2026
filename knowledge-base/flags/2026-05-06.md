# Flags raised 2026-05-06

Standing duty: surface points warranting careful PI review (per
[KB README](../README.md#claudes-standing-duties)).

---

## F-1 — TabPFN: hypothesis-board credits framework, mechanism was PI override + Rule 2

**Where**: `CLAUDE.md` `mechanism_families_explored:` and the calibration
ladder, `tabpfn_finetune_v25_v26 # Day-14`.

**What it says**: "DEAD. AUC ceiling 0.944 = -64bp vs PRIMARY... gap
too large for pool."

**What actually happened** (verified via git log on 2026-05-06):
- Rule 2 was active; agent iterated kernel v7→v9 toward smoke-first.
- TabPFN fold-0 smoke at 08:11 UTC: AUC 0.94439 → DEAD.
- BOTE/Rule 19 didn't exist yet — created at 09:57/10:26 UTC.
- PI explicitly insisted "test first" (override of agent's default
  full-5-fold plan) — this was load-bearing.

**Why it matters**: a casual reader (human or future agent) infers from
the hypothesis-board entry that the rules were sufficient to kill
TabPFN cleanly. They were not. The case credits a mechanism (BOTE)
that was not yet in existence; the actual mechanism (Rule 2 +
PI override) is invisible.

**PI implication**: when surveying "wins" attributable to the framework,
mentally subtract entries where PI override was the operative cause.
Otherwise the framework's measured efficacy is inflated.

**Suggested resolution** (PI to decide, not actioning): hypothesis-board
or audit entries could include a `pi_override: yes/no` field, plus
a `mechanism: rule2 / bote / rule16 / pi_override / …` tag, so that
calibration of *the framework* is separable from PI's manual catches.

---

## F-2 — Standing watch: Rule 19/BOTE itself was agent-authored

**Where**: commit `e48820d 2026-05-06 codify experimentation harness:
CLAUDE.md Rule 19 + HANDOVER refresh`, merged from
`claude/ml-handover-alignment-xvUN0`.

**Why noting**: not a flag for PI action. Filing it here as a recurring
pattern. The framework's most-trusted focus rule was drafted by an
agent and ratified by PI implicitly (via merge). This is the literal
mechanic by which CLAUDE.md grows. Confirms F1.3 finding empirically.

**Watch for**: future Rule 20+ similarly arriving as merges from
agent-feature branches without explicit PI authorship. Expected — flag
only if a rule introduces vocabulary or thresholds PI hasn't seen.
