# Per-competition config. Source from bootstrap.sh.
# Rename this file to .comp.env (drop the .template suffix).
# Add .comp.env to .gitignore — it's machine-local.

COMP="playground-series-sNNN"
ARTIFACT_DATASET="<your-kaggle-username>/sNNN-artifacts"
